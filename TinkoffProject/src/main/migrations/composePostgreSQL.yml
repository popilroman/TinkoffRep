version: '3.9'

services:
  postgresql:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_USER: popilroman
      POSTGRES_PASSWORD: popilroman
      POSTGRES_DB: scrapper
    volumes:
      - postgresql-data:/var/lib/postgresql/data
    networks:
      - backend
    ports:
      - "5432:5432"

  liquibase-migrations:
    image: liquibase/liquibase:4.18
    depends_on:
      - postgresql
    command:
      - --hub-mode=off
      - --changelog-file=master.xml
      - --driver=org.postgresql.Driver
      - --url=jdbc:postgresql://postgresql:5432/scrapper
      - --username=postgres
      - --password=postgres
      - update
    volumes:
      - ./migrations:/liquibase/changelog
    networks:
      - backend

volumes:
  postgresql-data:

networks:
  backend: